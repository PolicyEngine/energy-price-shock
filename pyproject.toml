[project]
name = "energy-shock"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "policyengine-uk",
    "microdf",
    "pandas",
    "numpy",
]

[tool.setuptools.packages.find]
include = ["energy_shock*"]
